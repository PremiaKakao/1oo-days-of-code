def add_digits(value=input("Please enter a number")):
    if isinstance(value, int):
        pass
    str_value = str(value)
    if len(str_value) != 2:
        return "Please enter a number with two digits"
    return int(str_value[0])+int(str_value[1])


print(add_digits())
